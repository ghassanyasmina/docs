---
openapi: post /insurance-aggregator
---
Hello
<Panel>

<CodeGroup>

```curl icon="square-terminal" cURL
curl --request POST \
  --url https://staging.yasmina.ai/api/v1/car/insurance-aggregator \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
  "owner_id": "0123456789",
  "car_year": 2020,
  "car_sequence_number": "123456789",
  "car_price": 150000,
  "insurance_type": "comp"
}'
```

```php icon="php" PHP
<?php

$curl = curl_init();

curl_setopt_array($curl, [
  CURLOPT_URL => "https://staging.yasmina.ai/api/v1/car/insurance-aggregator",
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_ENCODING => "",
  CURLOPT_MAXREDIRS => 10,
  CURLOPT_TIMEOUT => 30,
  CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
  CURLOPT_CUSTOMREQUEST => "POST",
  CURLOPT_POSTFIELDS => json_encode([
    'owner_id' => '0123456789',
    'car_year' => 2020,
    'car_sequence_number' => '123456789',
    'car_price' => 150000,
    'insurance_type' => 'comp'
  ]),
  CURLOPT_HTTPHEADER => [
    "Authorization: Bearer <token>",
    "Content-Type: application/json"
  ],
]);

$response = curl_exec($curl);
$err = curl_error($curl);

curl_close($curl);

if ($err) {
  echo "cURL Error #:" . $err;
} else {
  echo $response;
}
```

</CodeGroup>

</Panel>

<RequestExample dropdown>

```javascript icon="square-js"
const hello = "world";
```

```shell icon="square-js" Shell <Icon icon=square-js dropdown>
const hello = "world";
```
</RequestExample>

<CodeGroup>

```javascript icon="square-js" Javascript
const hello = "world";
```

```shell icon="square-js" Shell
const hello = "world";
```

</CodeGroup>
